import React from "react";
import Layout from "@jbolda/gatsby-theme-homepage/src/components/layout";

const Homepage = props => <Layout {...props}>{props.children}</Layout>;

export default Homepage;
